{
  "name": "witherapp",
  "version": "1.0.0",
  "description": "hybrid nuxt app for serving and creating pages",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nuxi dev --port 5000",
    "build": "nuxi build",
    "start": "node .output/server/index.mjs"
  },
  "devDependencies": {
    "less": "^4.1.2",
    "nuxt3": "latest"
  },
  "dependencies": {
    "@urql/vue": "^0.6.1",
    "graphql": "^14.7.0"
  }
}
